#!/bin/bash
set -e

if [ "$1" == "TOP" ]
then
	mkdir -p gpg-mailgate
	mkdir -p mmail
	pushd mmail 2>/dev/null
	ln -sf ../../../etc .
	ln -sf ../../../perl .
	ln -sf ../../../sh .
else
	git clone https://github.com/ajgon/gpg-mailgate.py gpg-mailgate
	git clone $HOME/mmail
	git checkout $1
fi

